{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "shotpilot/config.schema.json",
  "$comment": "Contract for config.json (machine config; only deviations from config.DEFAULTS). Writer: pipeline/config.py (validated before write); explicit check: `shot validate`.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "input_dir": {"type": "string", "minLength": 1},
    "jobs": {"type": "integer", "minimum": 0},
    "hwaccel": {"enum": ["auto", "off"]}
  }
}
